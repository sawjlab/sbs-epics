Miscellaneous drivers, extensions and libraries.

When adding new modules or versions, update drivers/Makefile to include it in 
automated builds.  Any dependencies can also be set within the Makefile.
